class CerticateAuthority():

    def __init__(self, name, publicKey):
        self.name = name
        self.publickey = publicKey 
    
    def directory(self):
        directory = []

    def getRegistered(self, name, publicKey):
        directory.append(directory(name, publicKey)

    def getValidated(self, )

